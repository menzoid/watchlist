<%= simple_form_for(@list) do |t| %>
  <div class="form-inputs">
    <%= t.input :name %>
  </div>
  <div class="form-action">
    <%= t.button :submit %>
  </div>
<% end %>

<%= link_to "Back to lists", lists_path %>
